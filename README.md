# Hangman Backend

Backend for Hangman

## Setup

Create venv and install dependencies

```bash
python3 -m venv env
source env/bin/activate
pip install -r requirements.txt
```

## Run

```bash
flask run